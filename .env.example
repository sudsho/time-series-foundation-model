# copy to .env and edit. .env is gitignored.

# path to a finetuned forecaster checkpoint
MODEL_CKPT=artifacts/finetune/best.pt

# mlflow tracking (optional; default: local file)
MLFLOW_TRACKING_URI=file:./mlruns

# uvicorn
PORT=8000
